

// auth options: OAuth, AWS, Firebase, home brew?
const handler = async(req, res) => {
    switch(res) {
        case 'GET':
            //get stuff
            break;
        case 'POST':
            // post stuff
            break;
    }
}
